# BMA — compact phiên làm việc (14/09/2026)

> Chỉ ghi lại thông tin còn hữu ích cho phiên tiếp theo. Phần “đã PASS” dựa trên báo cáo kiểm thử của chủ dự án trong phiên chat; trạng thái PR/CI được đối chiếu với GitHub ngày 14/09/2026. Không lưu secret, nội dung clipboard, dữ liệu cá nhân hay log lỗi cũ.

1. **Repo và nhánh:** `nguyenmian3012-code/BMA_BinhMinhApp`, `feature/engineering-alpha-v1`; PR [#1](https://github.com/nguyenmian3012-code/BMA_BinhMinhApp/pull/1) vào `main` vẫn **open / Draft / chưa merge**. Lúc đối chiếu, HEAD là `991da16ca0da509bfbc5ad82713caa1798ff3597`; không tiếp tục dùng `94f1646`, `f9c3888` hoặc `0887577` như HEAD hiện tại.
2. **Alpha Gate:** Chủ dự án báo cáo checkpoint 3, 4 và các bài kiểm thử Alpha cuối cùng đều PASS. Android thật, đăng nhập/Remember Me, các module được thử, kiểm tra sao lưu–khôi phục đều đã qua; lỗi `pg_restore`/EF migration history trước đó là lịch sử, không xem là blocker còn mở nếu không tái hiện.
3. **Thông báo mobile:** Sau sửa và kiểm tra, `BadgeWithin30Seconds`, `InboxAutoLoad`, `UnreadDarkBackground`, `ReadPaleBackground`, `ReadStatePersistence` đều PASS; `BlockingIssue: NONE` theo báo cáo thiết bị. Admin Dashboard xóa thông báo và tùy chọn Remember Me cũng PASS. Không quay lại lỗi chưa load/“Đã đọc” mặc định cũ nếu không có regression mới.
4. **Android signing:** Artifact Alpha đã qua checksum và cài/test trên thiết bị thật; repository secret cho ký Android đã được chủ dự án cập nhật. Không chia sẻ hoặc ghi khóa, base64 keystore, mật khẩu ký trong repo/chat.
5. **Staging:** Docker BMA + PostgreSQL từng được báo healthy; `/health`, Admin login và stylesheet đã HTTP 200. Phân biệt staging `127.0.0.1:8791/bmapp-staging` với production local `127.0.0.1:8790/bmapp`. Không suy ra gateway public đang live từ kết quả localhost.
6. **Nguồn BMKCS:** Trong phiên chat chưa có kết quả BMKCS đã publish để kiểm thử luồng đó. Không tự nhận phần tích hợp BMKCS end-to-end đã PASS chỉ vì module ứng dụng hoặc CI xanh.
7. **Chấm công:** Quyết định nghiệp vụ: Entry/Exit **từ Terminal vật lý** là nguồn chấm công; không lấy GPS/Wi-Fi/điện thoại làm nguồn. Theo PR hiện hành, Bridge raw-first, contract `EMPLOYEE_ENTRY/EMPLOYEE_EXIT` và test CI đã có; **chưa có bằng chứng callback vật lý IN+OUT, mapping person ID thật → `BM001`, ghi đủ PostgreSQL và hiển thị BMA**.
8. **Trạng thái code được GitHub ghi nhận:** Mô tả PR ở HEAD trên nêu .NET + PostgreSQL, canonical ingest/outbox, Flutter, native PostgreSQL cutover và terminal contract; báo cáo CI **BMA CI #30 SUCCESS**. Đây là bằng chứng source/CI theo PR, không thay cho chạy kiểm thử hardware hoặc nghiệm thu production.
9. **Ranh giới triển khai:** Engineering Alpha PASS **không đồng nghĩa Production GO**. Giữ PR Draft/chưa merge; không vô hiệu Dahahi, không cho Terminal/Bridge ghi thẳng SQL và không replay shadow cũ tự động trước khi đối soát, phê duyệt kế hoạch chuyển đổi.
10. **Việc còn cần đo và chứng minh:** Health gateway staging chính thức; callback thật của Terminal `1605063` từ nguồn đã khai báo; person ID thật → `BM001`; cặp IN/OUT qua `raw_integration_events`, `attendance_events`, `attendance_sessions`; xác nhận Công/Cá Nhân trên BMA; CI cuối vẫn xanh. Đối chiếu nguồn BMKCS publish khi dữ liệu sẵn có.
11. **Các gate sản phẩm sau Alpha:** Tiếp tục pilot Android, kiểm tra bảo mật/offline/push, chính sách privacy/xóa tài khoản/support; iOS cần macOS/Xcode, Apple Developer, APNs và iPhone thật. Công thức lương/tăng ca/nghỉ phép và recovery cũng cần chủ nghiệp vụ xác nhận trước Production Gate.
12. **Cách tiếp tục phiên sau:** Đọc PR #1 và tài liệu trong `docs/` tại HEAD thực tế, kiểm tra CI/runtime mới nhất, bắt đầu từ bằng chứng Terminal/gateway còn thiếu. Ghi rõ `PASS`, `BLOCKED`, `CHƯA THỬ` theo từng gate; làm theo lát ~10 phút/lần khi triển khai. Không coi một log lỗi đã sửa là trạng thái hiện tại.
